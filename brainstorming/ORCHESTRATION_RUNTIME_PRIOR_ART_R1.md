# Brainstorm — Orchestration Runtime prior-art findings

Date: 2026-09-23
Scope ID: orchestration-runtime-prior-art
Revision: R1
Status: tentative

## Goal

Receive and organize the orchestration-layer findings transferred from the completed architecture research in `elmakus/chatgpt-codex-project-workflow`.

This scope is deliberately narrower than the source research. It concerns the standalone `elmakus/orchestration-runtime` project only.

## Boundary

Project Workflow remains the governance/policy authority.

This repository concerns the execution layer that consumes an exact Project Workflow obligation and executes it through bounded workers/tools/sessions.

Working boundary:

```text
Project Workflow
  -> typed Execution Obligation
  -> Orchestration Runtime
  -> typed Execution Result
  -> Project Workflow
```

The runtime may execute repository/tool mutations when authorized by the obligation, but it must not become a second source of project lifecycle truth.

## Transferred source

Primary source artifact:

`elmakus/chatgpt-codex-project-workflow`
branch: `work/pwv2-architecture-prior-art-research`
artifact: `research/PWV2_ARCHITECTURE_PRIOR_ART_R3.md`
source blob: `3fbefb2fa7bb1e5ba9fecf08e61d59b4b33d5b6d`

R1/R2 source research pinned the upstream implementations; R3 rechecked that their main HEADs had not changed before performing the layer reclassification.

## Verified findings received

- Project Workflow and runtime orchestration are separate layers by design.
- Runtime should consume an exact semantic obligation rather than infer the current project lifecycle from broad repository/chat context.
- Runtime state/journals/sessions/worktrees are execution metadata and optimization, never canonical project authority.
- The strongest concrete Pi-native substrate candidate found is `pi-extensible-workflows`.
- Dagu provides strong deterministic execution-engine prior art but is less naturally embedded in Pi.
- pi-fabric provides strong code-mode composition and lazy capability-discovery prior art.
- Laya/System One is an optional advisory optimization for bounded typed classification, not a routing authority.
- rpiv-todo contributes only runtime-adjacent derived queue/UI ideas; its task persistence model must not become canonical runtime/project state.
- Existing `elmakus/codex_workflow` contains useful lessons around bounded delegation, provider/runtime abstraction, independent verification and event-driven waiting, but it is not the target architecture.
- Fresh independent review is a cross-layer contract: Project Workflow defines semantic freshness/evidence; runtime enforces clean session/resource isolation.

## Candidate families received

### Orchestration Runtime

- provider-neutral role runtime;
- explicit fresh/continuation worker sessions;
- bounded context materialization/transport;
- structured result transport;
- fan-out/fan-in/pipeline scheduling;
- lazy capability discovery;
- code-mode mechanical composition;
- runtime journal/replay;
- worktree isolation/mutation ownership;
- event-driven lifecycle and observability;
- budgets/timeouts/recursion/concurrency guards;
- optional calibrated typed decision helper;
- derived runtime/working-queue UX.

### Shared Project Workflow ↔ runtime interface

- typed Execution Obligation;
- typed Execution Result;
- fresh independent-review contract;
- abstract capability policy;
- canonical stale-binding handshake;
- user/checkpoint gate handshake;
- stable runtime failure taxonomy.

## Explicit rejects received

- runtime journal/run ID as project authority;
- external orchestrator database/history required for project recovery;
- runtime selecting canonical requirements/decisions/route by inference;
- bundled `pi-extensible-workflows` `reviewLoop` as Project Workflow independent review;
- ad-hoc LLM-generated workflow program as Project Workflow policy;
- persistent agent handle for fresh independent review;
- small classifier as legal workflow router;
- confidence threshold as proof of correctness;
- runtime-owned second Task Board;
- runtime worktree branch as replacement for Project Workflow workstream identity.

## Adaptive discovery state

### Accepted exploratory choices

| Choice | Challenge | Stability |
|---|---|---|
| Runtime remains separate from Project Workflow governance. | Combining them would reduce one interface but make execution state compete with canonical policy/state. | Stable research constraint. |
| Runtime should be provider-neutral. | Current codex_workflow proves multi-runtime roles are possible; retaining Codex-specific identity would make the new layer unnecessarily narrow. | Stable research conclusion, not yet requirement. |
| Runtime state is disposable relative to project truth. | Journals materially improve recovery speed, but requiring them would create a second authority. | Stable research conclusion. |
| Evaluate pi-extensible-workflows before rebuilding all Pi orchestration primitives. | Building from scratch gives control, but duplicates existing session/replay/worktree/schema machinery. | Stable candidate, not selected architecture. |
| Interface must carry semantic constraints without leaking provider-specific wiring into Project Workflow. | Putting tool names/model aliases into policy would couple governance to one runtime. | Stable research conclusion. |
| Main should retain a worker-free fast path for genuinely small/leaf work and decide whether delegation is worthwhile before spawning any subagent. | Forcing a subagent for every task wastes coordination/context on trivial work; allowing Main to do substantive delegated work would defeat the orchestration objective. | User-accepted exploratory choice from grilling round 1. |
| All normal worker roles should resolve through one centrally configured fixed worker model rather than role-specific model selection by Main. | Per-call model override or silent fallback to Main would make cost/capability behavior unpredictable. | User-accepted exploratory choice from grilling round 1; exact failure/fallback semantics still to refine. |
| Full Research should default to three fresh independent researcher lanes with complementary angles, synthesized by Main rather than majority voting. | One researcher is cheaper, but materially weaker against blind spots; identical prompts to three workers would waste the fan-out. | User-accepted exploratory choice from grilling round 1. |
| Runtime should support both Project-Workflow-controlled obligations and lightweight ad-hoc Main delegation through one orchestration layer. | A PW-only runtime would unnecessarily limit normal Pi use; two unrelated runtimes would duplicate worker/session mechanics. | User-accepted exploratory choice from grilling round 1; common-envelope details unresolved. |
| Keep Scout and Researcher as distinct semantic roles: Scout maps existing local/project context; Researcher investigates unknowns, alternatives, external evidence and prior art. | Merging them makes permissions/prompts broader and increases duplicate discovery. | User-accepted exploratory choice from grilling round 2. |
| Broad context discovery should default to two fresh Scout lanes with complementary angles; narrow lookups may use Main directly or one Scout. | Always spawning two wastes coordination on trivial lookups; one lane is weaker for genuinely broad discovery. | User-accepted exploratory choice from grilling round 2. |
| Review requirement is supplied by Project Workflow in PW-controlled mode and decided by Main in ad-hoc mode; runtime does not force review for every worker task. | Mandatory review of every trivial task adds unnecessary cost; omitting policy-owned review would violate governance. | User-accepted exploratory choice from grilling round 2. |
| Ordinary review defects should use a repair loop bound to the same Worker and same Reviewer when possible; no direct sibling-agent messaging is required. | Direct peer messaging weakens Main/runtime observability and can create hidden coordination state. | User-accepted exploratory choice from grilling round 2; exact broker/auto-forward semantics still to refine. |
| Only Main/runtime may spawn subagents initially; worker-to-worker recursive spawning is forbidden. | Flat orchestration keeps cost, authority, context and failure propagation bounded. | User-accepted exploratory choice from grilling round 2. |
| New Scout, Researcher and Reviewer assignments start in fresh sessions without inherited Main transcript; they receive only bounded task capsules and authorized references. | Full transcript inheritance increases contamination and context cost. | User-accepted exploratory choice from grilling round 3. |
| A Worker session should persist across implementation and ordinary repair iterations for the same bounded task, and be replaced only for a new task or explicit replacement. | Continuity preserves local implementation reasoning and avoids needless rediscovery. | User-accepted exploratory choice from grilling round 3. |
| Ordinary recheck after repair should return to the same Reviewer session; a new formal independent-review subject may require a fresh Reviewer when Project Workflow freshness rules say so. | Same-reviewer recheck preserves defect context, while formal independence remains a PW-owned semantic constraint. | User-accepted exploratory choice from grilling round 3. |
| Main should receive compact structured results plus evidence references by default, not worker transcripts or bulky logs. | This minimizes Main context while preserving evidence drill-down when needed. | User-accepted exploratory choice from grilling round 3. |
| Automatic Worker↔Reviewer repair cycles may run up to four failed repair attempts before escalation to Main. | A finite retry ceiling prevents unbounded loops while allowing more recovery than a strict two-attempt limit. | User-accepted exploratory choice from grilling round 3; exact failure accounting and reset semantics still to define. |
| Mutating Workers should use isolated worktrees by default when multiple Workers run concurrently; a single Worker may operate directly on the authorized workstream branch when collision risk is absent. | Mandatory worktrees for every trivial mutation add overhead, while concurrent writes need isolation. | User-accepted exploratory choice from grilling round 4. |
| Main/runtime may execute multiple Workers concurrently only when write scopes are non-overlapping and dependency ordering permits it; overlapping or dependent mutation packages must serialize. | Parallelism is useful only when ownership can be made mechanically safe. | User-accepted exploratory choice from grilling round 4. |
| Every semantic role has a hard capability ceiling; Main may narrow but never expand a worker beyond that role ceiling. | This keeps role safety enforceable by runtime rather than prompt convention. | User-accepted exploratory choice from grilling round 4. |
| Every worker return uses a validated structured result envelope with bounded natural-language fields and explicit evidence/check/change/blocker data as applicable. | Free-form returns are harder to route, validate, retry and summarize consistently. | User-accepted exploratory choice from grilling round 4. |
| Main/runtime may cancel an in-flight worker batch when new evidence invalidates its question or canonical precondition; normal Scout/Researcher batches otherwise complete before synthesis. | Early cancellation saves wasted work only when the remaining assignments have become semantically stale, not merely because an early answer looks plausible. | User-accepted exploratory choice from grilling round 4. |
| Worker-model resolution should fail closed when the configured worker model is unavailable; no silent fallback to Main or another model. | Silent fallback breaks cost/capability expectations and obscures execution semantics. | User-accepted exploratory choice from grilling round 5. |
| Runtime may automatically retry only failures classified as transient/retryable infrastructure failures; semantic, permission, stale-precondition and invalid-schema failures require cause correction or Main intervention. | Blind retries can repeat invalid work or side effects. | User-accepted exploratory choice from grilling round 5. |
| With Paseo fixed as the execution backend, initial timeout policy should rely on Paseo lifecycle/error/completion signals rather than a custom OR inactivity watchdog. OR may apply a generous absolute hard ceiling and escalate long-running work, but should not cancel an agent merely because wall-clock time passed while Paseo still reports normal work. | Paseo already supervises provider processes and notifications; duplicating fine-grained liveness inference in OR would add complexity and risk killing healthy long jobs. | Revised recommendation after backend clarification in grilling round 16; exact hard-ceiling policy remains unresolved. |
| Worker failure returns a compact diagnostic envelope including failure class, attempt count, last meaningful error, session/worktree refs and side-effect certainty, while keeping full logs out of Main context by default. | Main needs enough information to decide without absorbing bulky runtime traces. | User-accepted exploratory choice from grilling round 5. |
| Main has no bypass for runtime mechanical guardrails such as capability ceilings, model pinning or workspace isolation; it must instead issue a new legal assignment/strategy. | Prompt-level discretion must not disable enforced safety and determinism. | User-accepted exploratory choice from grilling round 5. |
| Initial semantic role set is `Scout`, `Researcher`, `Worker`, and `Reviewer`. | These names are provider-neutral and map cleanly to discovery, external/problem research, bounded mutation, and independent verification. | User-accepted exploratory choice from grilling round 6. |
| Do not add a dedicated Archivist role in the initial runtime. | Project Workflow owns durable project state in PW-controlled mode; ad-hoc documentation can be assigned to a Worker until evidence shows a separate role is worthwhile. | User-accepted exploratory choice from grilling round 6. |
| Do not add a dedicated Evidence Auditor role initially. | One Researcher lane can be explicitly adversarial/evidence-focused; a separate role can be introduced only if repeated use proves necessary. | User-accepted exploratory choice from grilling round 6. |
| Main should receive only material lifecycle events by default: completed, blocked/needs-decision, failed, stale, and optionally a sparse long-running notice; routine progress remains runtime-local. | This avoids polling and Main-context churn while preserving intervention points. | User-accepted exploratory choice from grilling round 6. |
| Runtime should enforce a configurable global concurrency ceiling; initial default maximum active subagent sessions is 9, with excess work queued rather than rejected when otherwise valid. | This bounds accidental fan-out and host/provider pressure without constraining the semantic size of a work wave. | User-accepted exploratory choice from grilling round 6. |
| Main owns semantic delegation classification (`direct`, Scout, Researcher, Worker, Reviewer); runtime enforces only mechanical validity and role invariants. | A second semantic router inside runtime would duplicate Main reasoning and risk conflicting authority. | User-accepted exploratory choice from grilling round 7. |
| Once Main/PW marks an assignment as full Research, runtime requires exactly three independent Researcher lanes for that bounded problem. | This preserves deliberate multi-angle research while leaving the decision to invoke full Research with Main/PW. | User-accepted exploratory choice from grilling round 7. |
| Broad discovery marked as such requires exactly two independent Scout lanes; narrow lookup may use one Scout or direct Main inspection. | This mirrors the research fan-out pattern without wasting workers on trivial discovery. | User-accepted exploratory choice from grilling round 7. |
| Multi-lane Scout/Researcher fan-out requires one shared bounded problem plus a distinct explicit angle for every lane; identical prompts are invalid for mandatory fan-out. | Diversity should come from intentional evidence perspectives, not stochastic duplication. | User-accepted exploratory choice from grilling round 7. |
| Main may open additional Scout/Researcher waves when a completed wave exposes a new material question; each new full Research problem again follows the three-lane rule. | Research depth should be adaptive rather than globally limited to one wave. | User-accepted exploratory choice from grilling round 7. |
| In PW-controlled mode, Main may decompose and refine an Execution Obligation into legal subassignments but may not change its goal, authority, evidence requirements, capability bounds or canonical subject. | Main orchestrates execution but does not rewrite Project Workflow semantics. | User-accepted exploratory choice from grilling round 8. |
| Main supplies semantic task content, angles and ownership; runtime materializes the final worker capsule with model resolution, session mode, capability ceiling, workspace/worktree, IDs, schema, timeout and canonical bindings. | This separates reasoning from mechanical enforcement and keeps worker packaging consistent. | User-accepted exploratory choice from grilling round 8. |
| In ad-hoc mode, Main may choose task-specific scope and capabilities only within the selected role's hard ceiling. | Ad-hoc flexibility should not bypass enforced role safety. | User-accepted exploratory choice from grilling round 8. |
| Worker results must bind to the exact task/obligation input through an immutable digest and relevant canonical preconditions; changed subject/head/precondition makes the old result stale rather than acceptable by default. | This prevents accepting evidence produced for an obsolete execution target. | User-accepted exploratory choice from grilling round 8. |
| Runtime should persist disposable execution journal/session metadata for resume, diagnostics and observability, including run IDs, worker sessions, attempts, worktrees, events and result references; project recovery must not depend on it. | Runtime state accelerates continuation but must remain subordinate to Git/Project Workflow truth. | User-accepted exploratory choice from grilling round 8. |
| Runtime recovery always revalidates canonical state and task preconditions before attempting resume; mismatched bindings make the prior run stale. | Runtime continuation must never outrank current Git/Project Workflow truth. | User-accepted exploratory choice from grilling round 9. |
| If a Worker session disappears while its worktree/partial changes remain, a fresh replacement Worker may take over through an explicit handoff of current workspace state rather than pretending session continuation. | Session identity and workspace state are separate; preserving that distinction avoids hidden continuity assumptions. | User-accepted exploratory choice from grilling round 9. |
| Uncertain external side effects must not be blindly retried after crash; runtime returns `side_effect_uncertain` unless a safe read-back/idempotency check can resolve the state. | Blind retries can duplicate irreversible actions. | User-accepted exploratory choice from grilling round 9. |
| Results marked stale cannot satisfy the current obligation/acceptance; Main may only reuse them as advisory input to a new legal task. | This preserves exact subject/precondition binding while avoiding needless loss of potentially useful information. | User-accepted exploratory choice from grilling round 9. |
| Runtime should garbage-collect completed worktrees, sessions and journals after a retention window and only after required results/evidence references have been safely persisted outside ephemeral execution state. | Ephemeral execution artifacts should not accumulate indefinitely or become accidental authority. | User-accepted exploratory choice from grilling round 9; exact retention period unresolved. |
| Successful runs should retain a compact durable runtime record containing run/task identity, obligation digest when applicable, role/model, status, attempts, timestamps, final structured result, evidence refs and mutation/worktree summary, without making full transcripts part of the normal durable record. | This preserves useful diagnostics/accounting while keeping runtime state bounded and non-authoritative. | User-accepted exploratory choice from grilling round 10. |
| OR should not separately persist worker transcripts when Paseo is the fixed execution backend; diagnostic history/timelines remain Paseo-owned runtime data, while OR stores only orchestration-specific records and Paseo references. | Duplicating Paseo timelines would add storage, synchronization and lifecycle complexity without adding authority. | Revised after the user fixed Paseo as the always-used backend in grilling round 16; supersedes the earlier temporary-transcript idea from round 10. |
| Runtime should expose a live derived status view for active orchestration (for example researcher wave progress, repair attempt count and reviewer state) without duplicating Project Workflow Task Board semantics. | Operators need observability, but runtime status must remain execution-only derived state. | User-accepted exploratory choice from grilling round 10. |
| Runtime should collect per-worker and per-run model/token/time usage metrics. | Cost and rollout telemetry is necessary to evaluate whether delegation actually reduces expensive Main usage and where optimization is worthwhile. | User-accepted exploratory choice from grilling round 10. |
| Initial budgeting should provide metrics and optional soft warnings rather than hard token/cost termination; hard budget enforcement should wait for empirical thresholds. | Premature hard budgets risk killing useful work without evidence-based limits. | User-accepted exploratory choice from grilling round 10. |
| All semantic roles should receive one common task-capsule envelope with role-specific payloads rather than unrelated prompt formats. | A shared contract simplifies validation, observability and runtime composition. | User-accepted exploratory choice from grilling round 11. |
| Task capsules should include a short execution intent/rationale sufficient for local decisions, without copying Main's full reasoning history. | Workers need purpose and trade-off context, but not hidden chain-of-thought or broad conversation history. | User-accepted exploratory choice from grilling round 11. |
| In PW-controlled mode, authority delivery should be hybrid: materialize the most relevant bounded excerpts while also providing exact canonical refs/hashes for authorized drill-down. | Refs-only forces repeated rediscovery; full-copy context is wasteful and risks contamination. | User-accepted exploratory choice from grilling round 11. |
| Capsules should state only material negative boundaries explicitly, while capability enforcement remains mechanical in runtime. | Long prompt-level prohibition lists are brittle and duplicate enforceable policy. | User-accepted exploratory choice from grilling round 11. |
| Worker results should use one common base envelope plus small role-specific sections rather than one monolithic all-role schema or completely unrelated schemas. | Main/runtime need stable common fields, while role outputs differ enough that forcing every field everywhere would add noise. | User delegated this design choice in grilling round 11; recommendation accepted. |
| Main owns semantic angle generation for multi-lane Scout/Researcher waves; runtime validates lane count and distinct explicit angles but does not invent them. | Angle selection depends on problem understanding and belongs with Main reasoning. | User-accepted exploratory choice from grilling round 12. |
| Runtime may collect/bundle multi-worker results but must not semantically synthesize or resolve conflicts before Main sees them. | Semantic synthesis would turn runtime into a second reasoning authority. | User-accepted exploratory choice from grilling round 12. |
| Role definitions should be centrally versioned by orchestration-runtime so each run records the exact role contract used. | Versioned roles make behavior auditable and allow prompt evolution without ambiguity. | User-accepted exploratory choice from grilling round 12. |
| Main may provide task-specific guidance but may not override or append arbitrary role-system instructions that change role semantics or enforced boundaries. | Stable role identity and safety must belong to runtime rather than per-task prompt discretion. | User-accepted exploratory choice from grilling round 12. |
| In PW-controlled mode, Main may spawn additional Scouts/Researchers not explicitly requested by PW when they remain within the current obligation's goal, authority and capability boundaries. | PW constrains legal outcome and authority; Main should retain execution-strategy freedom inside those bounds. | User-accepted exploratory choice from grilling round 12. |
| Runtime contracts and durable schemas should remain provider-neutral even if the first concrete execution adapter targets Pi. | Provider-specific names in the shared contract would unnecessarily couple PW/runtime semantics to one host. | User-accepted exploratory choice from grilling round 13. |
| Worker model selection should resolve through one centrally configured global worker alias/profile rather than being embedded separately in each role definition. | A single model policy makes worker-cost behavior predictable and easy to change centrally. | User-accepted exploratory choice from grilling round 13. |
| Reasoning effort should also be fixed centrally for all normal worker roles rather than varying by role. | User prefers one stable worker execution profile over role-specific tuning. | User-accepted exploratory choice from grilling round 13. |
| Capability policy should use abstract role/runtime concepts rather than concrete Pi tool names wherever practical. | This preserves portability and keeps policy separate from adapter wiring. | User-accepted exploratory choice from grilling round 13. |
| Do not require fail-closed cancellation solely because every capability ceiling cannot be mechanically enforced; prefer simple/best-effort enforcement plus explicit prompt/runtime boundaries rather than overengineering. | Some host capabilities may not expose perfect enforcement; complexity should be justified by real risk/evidence. | User-accepted exploratory choice from grilling round 13; exact minimum enforceable boundaries remain to define. |
| Evaluate Paseo as a possible execution/lifecycle substrate beneath orchestration-runtime, not merely an observability UI: OR would retain semantic roles, task capsules, fan-out invariants and PW boundary while Paseo could provide provider adapters, agent lifecycle, workspaces/worktrees, parentage, stop/resume and cross-device observability. | Rebuilding mechanics Paseo already provides may be unnecessary; however Paseo must not become Project Workflow authority or replace OR semantic contracts. | New research finding during grilling round 13; architecture choice not yet accepted. |
| Paseo is the mandatory initial OR execution/control backend for the user's deployment, while Pi remains the coding-agent harness/provider underneath it. Shared OR contracts should still avoid unnecessary Paseo-specific semantics so a future adapter remains possible. | The user clarified that Pi will always be operated through Paseo on Android/PC and not directly from a terminal. | Revised user decision from grilling round 16; supersedes the earlier round-14 backend-neutral deployment assumption. |
| Main should orchestrate workers through the OR contract rather than raw Paseo-specific agent commands, even when the human operates Pi through Paseo. | Direct vendor/client commands from Main could bypass OR role, fan-out, model and lifecycle invariants; Paseo can remain an operator UI around the same sessions. | User-aligned recommendation from grilling round 14. |
| The global fixed worker profile should map cleanly into the active Pi/Paseo execution environment while remaining centrally owned by OR configuration. | This preserves one worker model/effort policy regardless of which UI launches or observes the Pi harness. | User-accepted exploratory choice from grilling round 14. |
| Use Paseo's existing GUI on Android/PC for human observability and control instead of building a separate full OR dashboard initially. | Reusing an existing preferred UI avoids duplicating session and monitoring UX. | User-accepted exploratory choice from grilling round 14. |
| Paseo session/timeline/workspace state is runtime/control-plane metadata only and remains disposable relative to Git/Project Workflow canonical state. | GUI/session state must never become project authority. | User-accepted exploratory choice from grilling round 14. |
| OR should expose its Main-facing entry point as a Pi extension/tool surface, with core orchestration logic kept in a separate testable library. | The user will run Pi through Paseo rather than terminal Pi, so the Pi extension remains the natural harness integration while avoiding burying all semantics in UI/client code. | User-accepted exploratory choice from grilling round 15. |
| The initial concrete execution path should target Paseo-managed Pi sessions; direct-terminal/direct-Pi execution may remain an adapter possibility but is not an initial implementation priority. | The user explicitly expects to use Paseo on Android/PC and not operate Pi from the terminal. | User clarification from grilling round 15. |
| Main should see one high-level OR delegation/orchestration tool rather than separate spawn tools per semantic role. | A single typed entry point centralizes validation and keeps the Main tool surface small. | User-accepted exploratory choice from grilling round 15. |
| Do not design an externally-steered subagent state for initial scope; the user intends to observe rather than manually inject messages into worker conversations. | Supporting a behavior the user does not expect to use would add lifecycle complexity without current value. | User-rejected proposal from grilling round 15. |
| Main should automatically use OR whenever it judges delegation worthwhile; the user should not need to explicitly request subagents for normal operation. | This preserves the desired fast-path-vs-delegation judgment inside Main rather than in user commands. | User-accepted exploratory choice from grilling round 15. |
| OR-managed workers should be created as Paseo-managed subagents so they appear as full children in the Main agent's Paseo Subagents track and inherit Paseo lifecycle/notification behavior. | The user will always operate Pi through Paseo and wants the GUI to expose worker activity. | User-accepted exploratory choice from grilling round 16. |
| Normal workers should use one shared Paseo launch profile corresponding to OR's fixed worker model and fixed reasoning effort. | This matches the accepted one-worker-profile policy while using Paseo's native launch configuration surface. | User-accepted exploratory choice from grilling round 16. |
| PW branch isolation and Paseo workspace isolation are separate concerns: read-only roles may share the current workstream-branch workspace; concurrent mutating workers should receive Paseo worktrees/subbranches based on the current workstream branch, never branch directly from `main` for managed PW implementation. | Project Workflow requires branch-first work and PR merge to main; worktrees solve concurrent filesystem/write isolation, not workflow authority. | Refined recommendation from grilling round 16; user confirmed branch-first/PR-only main policy, exact single-worker placement remains as previously accepted. |
| OR should consume Paseo completion/error/attention notifications rather than polling worker status. | Paseo already provides asynchronous child lifecycle notifications and the user will always run through Paseo. | User-accepted exploratory choice from grilling round 16. |
| OR should assign descriptive Paseo child titles that expose semantic role and lane angle in the GUI. | The user wants Paseo Android/PC observability; readable names make multi-agent waves understandable at a glance. | User-accepted exploratory choice from grilling round 16. |
| With Paseo fixed as the execution/control plane, OR should not duplicate Paseo's agent timelines/transcripts/workspace lifecycle data; its durable runtime record should store orchestration-specific state plus Paseo agent/workspace references. | Duplicate lifecycle storage would add drift and complexity while Paseo already persists resumable agent state. | Revised recommendation after backend clarification in grilling round 16. |
| Paseo exposes multiple delegation surfaces (managed `create_agent`, CLI `paseo run`, provider-native children for supported providers, and higher-level advisor/committee/handoff skills), but OR should standardize on Paseo-managed child creation rather than rely on provider-native child semantics. | Managed children give consistent full-session GUI, parentage, workspace placement, notifications and lifecycle controls across providers. | New research conclusion during grilling round 16; implementation detail not yet promoted. |
| Current Project Workflow `chatgpt_only` already makes managed work branch-first: each workstream has one exact branch, mutable managed work must recover/create that branch-isolated workstream, and integration to the target/main occurs only through the workstream integration lifecycle. | This means OR/Pi should consume and enforce the selected workstream branch rather than invent a parallel branch policy. | Verified against current PW main during grilling round 17. |
| Current PW seriality is one in-progress Card per selected workstream and states that concurrency exists between workstreams, not between Cards; OR parallel read-only workers fit naturally, but multiple mutating Workers inside one Card/workstream require an explicit compatibility decision for PWv2.1 rather than assumption. | OR must not accidentally create a second execution lane that contradicts PW governance. | New integration constraint discovered during grilling round 17. |
| Global Pi/OR default should allow read-only inspection on `main` but prohibit repository mutation from starting on `main`. | This gives a safe baseline outside PW without forcing branches for pure inspection. | User-accepted exploratory choice from grilling round 17. |
| In ad-hoc non-PW work, when Main needs to mutate a repository while currently on `main`, OR should create an isolated task branch/worktree automatically before the first write. | Users should not need to remember branch hygiene manually. | User-accepted exploratory choice from grilling round 17. |
| Main fast-path work remains subject to the same Git mutation policy; skipping delegation does not permit writes on `main`. | Fast path changes execution topology, not repository safety policy. | User-accepted exploratory choice from grilling round 17. |
| PWv2.1 should explicitly decide whether one active Card/Execution Obligation may internally fan out to several mutating Workers while remaining one acceptance subject; current brainstorming preference is yes when ownership/scope remain one obligation. | This preserves PW serial governance while allowing OR execution parallelism. | User-accepted exploratory choice from grilling round 17; follow-up issue to be created in the PWv2 repo after Brainstorming completes. |
| Multiple mutating Workers inside one bounded assignment must use separate Paseo worktrees/subbranches derived from the current canonical workstream branch. | Concurrent writes must never share one mutable checkout. | User-accepted exploratory choice from grilling round 17. |
| Each mutating Worker should end with its own local commit rather than only leaving a dirty worktree. | Commit identity gives an exact integration/review subject and simplifies rollback. | User-accepted exploratory choice from grilling round 17. |
| Main owns the semantic decision to integrate worker output; OR may perform the mechanical merge/cherry-pick into the canonical workstream branch and must escalate conflicts or ambiguity to Main. | This keeps semantic acceptance with Main while automating routine Git mechanics. | User-accepted exploratory choice from grilling round 17. |
| Reviewer should inspect the exact Worker commit/subbranch in a fresh session without Worker transcript contamination, using the same files/worktree subject as needed. | Review independence depends on fresh context, not a duplicate checkout. | User-accepted exploratory choice from grilling round 17. |
| Final PR/integration to `main` must originate from the canonical PW workstream branch, never directly from temporary OR worker subbranches. | Worker branches are execution mechanics; PW workstream identity remains the integration surface. | User-accepted exploratory choice from grilling round 17. |
| After Brainstorming completes, create a `pi-unraid` follow-up issue to verify/globalize Pi repository mutation policy: PW authority when present, no writes on `main`, and automatic ad-hoc task branch/worktree creation before mutation. | This belongs in the Pi host/deployment setup rather than OR architecture alone. | User-requested deferred follow-up from grilling round 17. |
| In PW-controlled mode, the exact PW-selected workstream branch is the canonical branch identity; OR must not execute the obligation on an unrelated existing checkout/branch, but may create controlled worker subbranches/worktrees derived from that canonical branch. | PW owns branch identity while OR may add temporary execution isolation beneath it. | User delegated this design choice in grilling round 17; recommendation accepted. |
| Completed Paseo workers should not be auto-archived immediately on first response; OR first validates the structured result and preserves sessions needed for bounded repair/recheck, then archives after assignment closure. | Premature archive would break continuation loops. | User-accepted exploratory choice from grilling round 18. |
| Read-only Scout and Researcher roles should normally share the Main/current workstream Paseo workspace rather than receive isolated worktrees. | Read-only discovery does not benefit from filesystem duplication. | User-accepted exploratory choice from grilling round 18. |
| A single mutating Worker does not require a separate worktree when no competing mutation exists; it may use the canonical workstream workspace. | Worktrees are an isolation mechanism, not a mandatory per-agent ritual. | User-accepted exploratory choice from grilling round 18. |
| While a mutating Worker owns a shared workspace, Main should avoid mutating that same workspace; independent Main mutation must wait or use separate isolation. | Shared mutable checkout ownership should be unambiguous. | User-accepted exploratory choice from grilling round 18. |
| Paseo `notifyOnFinish` should remain enabled for all normal OR workers; basic OR does not use fire-and-forget delegation. | Completion and failure delivery should be event-driven and reliable. | User-accepted exploratory choice from grilling round 18. |
| A Worker that returns blocked/needs-decision for the same bounded task should remain resumable in the same session after Main resolves the decision. | Preserving implementation context is valuable when task identity has not changed. | User-accepted exploratory choice from grilling round 18. |
| Scout/Researcher follow-up should normally be a fresh bounded assignment for substantive new inquiry; reuse the same session only for minor clarification of the just-returned lane result. | Fresh analytical lanes reduce context carryover while avoiding pointless restarts for tiny clarification. | User-accepted exploratory choice from grilling round 18. |
| Reviewer should be archived after GREEN once verdict/evidence are durably captured. | A completed accepted review has no continuation need. | User-accepted exploratory choice from grilling round 18. |
| Reviewer should remain in the same session across the bounded Worker repair loop after RED, up to the configured repair-attempt limit. | Same-subject recheck benefits from continuity while preserving independence from the Worker's transcript. | User-accepted exploratory choice from grilling round 18. |
| OR should automatically clean up Paseo worker workspaces/worktrees after safe integration/closure and persisted result/evidence references rather than relying on manual GUI cleanup. | Automated cleanup keeps the Paseo UI and filesystem bounded without sacrificing recoverability. | User-accepted exploratory choice from grilling round 18. |
| Scout should be repo/project-context oriented and not receive external web access by default; external investigation belongs to Researcher. | This keeps Scout bounded to discovery of existing local/canonical context. | User-accepted exploratory choice from grilling round 19. |
| Researcher should remain read-only with respect to project files while being allowed to inspect repository state, documentation, external sources and safe read-only commands. | Research produces evidence/findings rather than implementation mutations. | User-accepted exploratory choice from grilling round 19. |
| Worker should normally have shell and repository-write capability; network access is available when task execution needs it rather than being universally assumed. | Worker is the implementation role and needs practical local execution power. | User-accepted exploratory choice from grilling round 19. |
| Reviewer must not modify production code during review; it may run tests/checks and use ephemeral test artifacts, while corrective production changes return to Worker. | Review independence and immutable subject boundaries matter more than convenience. | User-accepted exploratory choice from grilling round 19. |
| Reviewer should report incorrect tests rather than edit them as part of the review verdict; changing reviewed tests belongs to a corrective Worker assignment and creates a new subject. | Editing the evidence surface during review would contaminate the verdict. | User-accepted exploratory choice from grilling round 19. |
| Secrets/credentials should not be inherited wholesale from Main; OR should pass the minimum task-relevant credentials when the task legitimately requires them. | The user permits worker secret use, but prefers bounded task-scoped access rather than blanket inheritance. | User-accepted exploratory choice from grilling round 19. |
| Worker external side effects are allowed when the task/obligation explicitly authorizes them; ordinary repo-write permission alone does not imply production deploy/restart/API mutation authority. | This avoids accidental real-world mutation without making Worker artificially read-only or incapable. | Refined recommendation accepted in grilling round 19. |
| Worker subbranches should normally remain local; push is unnecessary unless a specific workflow requires remote publication of that temporary branch. | Canonical publication should stay centered on the workstream branch. | User-accepted exploratory choice from grilling round 19. |
| Final workstream PR creation/integration belongs to PW Close/Main semantics, with OR allowed to perform the mechanical GitHub operation only after the legal integration point is reached. | Worker completion is not equivalent to project integration authority. | User-accepted exploratory choice from grilling round 19. |
| In ad-hoc mode, local commit is automatic; push/PR/merge occur only when they are part of the user/Main goal. Every completion must surface an explicit integration state such as local-only, pushed, PR-open, or merged-to-main; generic `done` must never hide an unmerged branch. | The user must be able to distinguish implemented from integrated work at a glance. | User-accepted exploratory choice from grilling round 19 with explicit visibility requirement. |
| Generic `done` is insufficient for mutating work; completion must include an explicit integration state. | Implementation completion and repository integration are distinct lifecycle facts. | User-accepted exploratory choice from grilling round 20. |
| When implementation is complete but required review is still pending, expose `implementation_complete / review_pending` rather than project-complete wording. | Review gates remain real obligations after code stops changing. | User-accepted exploratory choice from grilling round 20. |
| A GREEN-reviewed canonical workstream branch with no PR yet should expose an `integration_ready` state, not merged status. | Review success does not imply publication. | User-accepted exploratory choice from grilling round 20. |
| An open unmerged PR must surface an explicit `PR_OPEN` state plus exact PR/branch identity and an explicit statement that the change is not yet on main. | This prevents user-facing ambiguity about deployment/integration state. | User-accepted exploratory choice from grilling round 20. |
| In PW mode, when the router makes merge the deterministic legal next step and no user gate exists, Main/OR should perform the merge automatically rather than stopping for a manual click. | Deterministic authorized workflow should continue to the real stop. | User-accepted exploratory choice from grilling round 20. |
| After final merge, OR/PW must verify target-side presence of the exact workstream/merge result before reporting `MERGED_MAIN`. | Merge API success alone is not enough for terminal truth. | User-accepted exploratory choice from grilling round 20. |
| Final mutating results should expose exact Git identity including workstream branch, final commit SHA, PR identity and merge SHA when applicable. | Exact refs make recovery and user verification cheap and unambiguous. | User-accepted exploratory choice from grilling round 20. |
| Worker completion state and whole-orchestration completion state must remain distinct. | Individual workers can finish while review, synthesis or integration is still outstanding. | User-accepted exploratory choice from grilling round 20. |
| After a worker commit is durably integrated into the canonical workstream branch, its temporary worktree/subbranch may be safely removed before the final PR merges. | Temporary execution isolation need not survive once canonical branch contains the accepted commit. | User-accepted exploratory choice from grilling round 20. |
| Every user-facing completion for mutating work should include a compact mandatory `Git state` field such as `MERGED_MAIN`, `PR_OPEN #42`, `INTEGRATION_READY`, or `IMPLEMENTED_LOCAL`. | The user explicitly wants a reliable at-a-glance indicator of whether work actually reached main. | User-accepted exploratory choice from grilling round 20. |
| A pre-mutation retryable provider failure should automatically restart the same assignment in a fresh worker session while preserving task identity. | Infrastructure retry should not require semantic replanning. | User-accepted exploratory choice from grilling round 21. |
| When a Worker session dies after partial uncommitted changes, a replacement session should first validate and then continue from the same worktree through explicit handoff rather than discard useful progress. | Workspace state can survive session identity. | User-accepted exploratory choice from grilling round 21. |
| If a Worker commit exists but the structured result was lost during a crash, OR may recover the result from Git plus Paseo history and must not rerun implementation solely because the final response was missed. | Durable code state outranks missing transient messaging. | User-accepted exploratory choice from grilling round 21. |
| A Reviewer that fails before a terminal verdict is replaced by a fresh Reviewer on the same immutable subject. | Partial review cannot satisfy independence or acceptance. | User-accepted exploratory choice from grilling round 21. |
| Main interruption/closure does not automatically cancel active Paseo workers; when they remain valid they may finish and Main later recovers from Paseo/OR/Git state. | Parent UI/turn lifecycle should not destroy useful delegated work by default. | User-accepted exploratory choice from grilling round 21. |
| Stopping the Main turn and cancelling the orchestration are distinct actions. | A user should be able to interrupt Main without killing long-running delegated work. | User-accepted exploratory choice from grilling round 21. |
| OR should expose an explicit whole-run cancellation action that stops the known child set for that orchestration through Paseo. | Run-wide cancellation needs deliberate semantics rather than piggybacking on Main interruption. | User-accepted exploratory choice from grilling round 21. |
| Cancellation must not immediately destroy dirty worker worktrees; preserve enough state for recovery/diagnostics before later cleanup. | Blind cleanup after cancel can destroy useful or ambiguous work. | User-accepted exploratory choice from grilling round 21. |
| OR may auto-resolve only clearly mechanical merge/cherry-pick conflicts that do not change semantics; ambiguous conflicts return to Main for decision or a bounded repair assignment. | Conflict resolution can alter intent and therefore needs semantic ownership when nontrivial. | User-accepted exploratory choice from grilling round 21. |
| A successful PR merge call without matching target-side readback yields `integration_uncertain`, never `MERGED_MAIN`, and routes to recovery/evidence reconciliation. | Terminal integration status requires verified target truth. | User-accepted exploratory choice from grilling round 21. |
| Main must resolve disagreements among Researcher lanes by comparing evidence and assumptions rather than using majority voting. | Research fan-out is for independent evidence gathering, not democratic selection. | User-accepted exploratory choice from grilling round 22. |
| When Researcher disagreement exposes a genuinely new full Research problem, Main may open a new bounded three-lane Research wave focused on that missing fact. | Adaptive depth should follow evidence gaps rather than fixed rounds. | User-accepted exploratory choice from grilling round 22. |
| A single strong, well-supported Researcher finding need not trigger another wave merely because other lanes found no contrary evidence. | Evidence weight and quality matter more than symmetry. | User-accepted exploratory choice from grilling round 22. |
| Main may reject even unanimous Researcher conclusions when it identifies a material flaw in their assumptions or evidence, and may request fresh verification. | Research workers inform Main; they do not replace Main semantic responsibility. | User-accepted exploratory choice from grilling round 22. |
| Researcher lanes in an initial wave must not see each other's results before returning their own findings. | Independence reduces convergence and anchoring bias. | User-accepted exploratory choice from grilling round 22. |
| Scout lanes likewise remain independent during their first pass and do not receive sibling findings before completion. | Complementary discovery is only useful when each lane maps context independently. | User-accepted exploratory choice from grilling round 22. |
| Main should synthesize Scout outputs into a compact context map before constructing later worker/research assignments. | Downstream capsules should use a coherent bounded project map rather than raw parallel transcripts. | User-accepted exploratory choice from grilling round 22. |
| Main synthesis is not a durable OR artifact by default; it becomes durable only when PW or project authority requires it as evidence/decision/research output. | Ephemeral semantic reasoning should not become accidental project authority. | User-accepted exploratory choice from grilling round 22. |
| OR must not auto-launch tie-breaker Researchers on semantic disagreement; Main decides whether a new bounded research problem exists and how to frame it. | OR remains mechanical and does not become a semantic router. | User-accepted exploratory choice from grilling round 22. |
| Research depth should have no hard numeric wave cap; runtime may surface soft warnings when repeated waves stop producing new evidence, leaving escalation/continuation to Main. | Hard limits could truncate legitimate difficult research while soft signals expose loops. | User-accepted exploratory choice from grilling round 22. |
| Raw Scout/Researcher outputs remain runtime results by default and become durable project evidence only when Main/PW explicitly promotes them into the current obligation's required evidence surface. | Internal delegation output should not automatically become canonical project state. | User-accepted exploratory choice from grilling round 23. |
| OR must not independently choose canonical `research/`, `evidence/`, `handoffs/` or similar destinations; PW/Main supplies the legal durable destination and contract, while OR may perform the mechanical write. | Durable project structure remains owned by Project Workflow. | User-accepted exploratory choice from grilling round 23. |
| When a PW obligation requires a specific evidence schema/path, OR should mechanically validate structure, required refs/checks and exact obligation binding before returning it, without making semantic sufficiency judgments. | Mechanical validation belongs in runtime; semantic acceptance belongs to Main/PW. | User-accepted exploratory choice from grilling round 23. |
| PW should not receive every internal subagent result; it receives only the result/evidence required by the active obligation, while internal Scout/Researcher/repair chatter stays within OR/Main. | This preserves bounded PW context and prevents execution noise from becoming governance state. | User-accepted exploratory choice from grilling round 23. |
| For formal PW Research composed from multiple Researcher lanes, Main performs semantic synthesis, PW defines the destination/format, and OR may mechanically write/commit the resulting artifact. | Each layer retains its proper responsibility. | User-accepted exploratory choice from grilling round 23. |
| OR Reviewer and PW Independent Review are distinct concepts: OR provides the executable reviewer role, while PW determines when that role satisfies a formal review obligation and which freshness/subject/evidence rules apply. | Runtime role identity does not itself create governance authority. | User-accepted exploratory choice from grilling round 23. |
| OR may satisfy a formal PW fresh-review obligation by creating a genuinely fresh Reviewer assignment that matches the exact PW review contract and has not participated in the subject as implementer/researcher. | Formal independence is a property of the assignment and subject history, not merely the role name. | User-accepted exploratory choice from grilling round 23. |
| Advisory OR reviews outside a formal PW obligation must not mutate Task Board formal review state. | Extra checking can inform Main without bypassing PW review lifecycle. | User-accepted exploratory choice from grilling round 23. |
| OR core should remain unaware of concrete PW concepts such as Card, Milestone, Definition or Planning; it consumes neutral obligations/subjects/authority/evidence contracts, with a PW adapter performing the mapping. | This preserves provider/governance neutrality in the runtime core. | User-accepted exploratory choice from grilling round 23. |
| All canonical durable mutations produced through OR/PW must continue through normal Git commits on the canonical workstream branch; OR must not introduce a parallel quasi-authoritative state store. | Git/PW remain the sole durable authority for project state. | User-accepted exploratory choice from grilling round 23. |
| Every run should record the exact semantic role-contract version used, not only the generic role name. | Versioned role identity makes historical execution behavior auditable. | User-accepted exploratory choice from grilling round 24. |
| Role-version increments are required for behaviorally meaningful prompt/contract changes, but not for cosmetic wording changes with no semantic effect. | Versioning should track behavior, not create noise. | User-accepted exploratory choice from grilling round 24. |
| `worker_default` may resolve dynamically for new runs, but each started assignment pins the concrete resolved execution profile for its lifetime. | Active work must not change model behavior mid-run because global configuration moved. | User-accepted exploratory choice from grilling round 24. |
| An ongoing Worker/Reviewer repair loop keeps the execution profile it started with even if the global worker profile changes during the loop. | Same bounded assignment should remain behaviorally consistent across repair/recheck. | User-accepted exploratory choice from grilling round 24. |
| Changing the global worker profile does not automatically restart existing idle/blocked assignments; new assignments use the new profile while existing ones retain their pinned profile. | Configuration changes should not rewrite in-flight task identity. | User-accepted exploratory choice from grilling round 24. |
| OR should record both its logical profile alias and the concrete provider/model/settings resolved through Paseo for each assignment. | Logical policy and actual executed configuration are both needed for diagnostics and audit. | User-accepted exploratory choice from grilling round 24. |
| OR role/configuration policy, including role versions, worker profile and concurrency defaults, should live in versioned repository configuration rather than only Paseo GUI settings. | Important runtime behavior needs durable reviewable source control. | User-accepted exploratory choice from grilling round 24. |
| OR configuration is authoritative for worker model/effort policy; Paseo Agent Profiles are execution materialization/convenience rather than policy authority. | GUI profile drift must not silently redefine runtime policy. | User-accepted exploratory choice from grilling round 24. |
| OR must detect Paseo profile/config drift and either materialize the expected parameters explicitly at agent creation or fail clearly; silent fallback is not allowed. | Execution should match the pinned OR contract. | User-accepted exploratory choice from grilling round 24. |
| Active runs should remain pinned to the runtime/config/role-contract versions that materially define their semantics; upgrades apply to new runs rather than mutating in-flight behavior. | Runtime upgrades must not make one assignment span incompatible execution contracts. | User-accepted exploratory choice from grilling round 24. |
| Main should use one primary typed `orchestrate(request)` entry point for delegation, with only small auxiliary lifecycle operations such as cancel/resume/status. | A compact tool surface centralizes validation without hiding necessary control. | User-accepted exploratory choice from grilling round 25. |
| `orchestrate()` should return promptly with a `run_id` and launch state while completion arrives asynchronously through Paseo events/notifications. | Long-lived worker execution should not block Main synchronously. | User-accepted exploratory choice from grilling round 25. |
| One orchestration request may describe a whole Scout/Researcher fan-out wave with explicit lanes so OR can validate mandatory lane count/angles atomically before launch. | Fan-out invariants are easier to enforce at batch creation than across unrelated calls. | User-accepted exploratory choice from grilling round 25. |
| Single-worker delegation uses the same orchestration API with a one-lane assignment rather than a separate execution system. | One contract should cover both scalar and fan-out delegation. | User-accepted exploratory choice from grilling round 25. |
| OR run state should remain a small execution state machine such as `created`, `running`, `blocked`, `completed`, `failed`, `cancelled`, `stale`, without mirroring PW lifecycle. | Runtime state should describe execution only. | User-accepted exploratory choice from grilling round 25. |
| Multi-lane Scout/Researcher batches may expose a derived `collecting` state while some lanes have completed and others remain active. | This improves observability without creating new governance semantics. | User-accepted exploratory choice from grilling round 25. |
| `blocked` is reserved for assignments that require new semantic/user/authority input to continue, while technical execution errors use `failed`. | Distinct states improve recovery routing and user understanding. | User-accepted exploratory choice from grilling round 25. |
| Worker→Reviewer→repair cycles remain under one logical `run_id` while individual attempts/assignments receive separate identities. | The whole repair loop is one execution objective with multiple bounded attempts. | User-accepted exploratory choice from grilling round 25. |
| A substantively new Research problem discovered from an earlier wave receives a new `run_id` linked through provenance such as `derived_from`. | Separate bounded questions should remain independently recoverable and auditable. | User-accepted exploratory choice from grilling round 25. |
| OR should expose compact on-demand run summary retrieval for recovery/manual inspection, while normal progress remains event-driven rather than polled. | Inspectability is still needed after restart or operator intervention. | User-accepted exploratory choice from grilling round 25. |
| OR should attach Paseo metadata labels such as run identity, semantic role, lane angle and workstream identity to managed workers. | Labels make OR-created agents filterable/recoverable without introducing a second UI. | User-accepted exploratory choice from grilling round 26. |
| Paseo worker titles should be short, human-readable role/task labels while technical identities remain in metadata. | The GUI is primarily for human inspection, so titles should optimize glanceability. | User-accepted exploratory choice from grilling round 26. |
| Multi-lane titles should include lane position, e.g. `Researcher 2/3 — ecosystem`, so the operator can see wave completeness at a glance. | Lane numbering improves fan-out observability. | User-accepted exploratory choice from grilling round 26. |
| Repair-attempt count should live in labels/runtime status rather than constantly changing the agent title. | Stable titles aid navigation while mutable attempt data belongs in metadata. | User-accepted exploratory choice from grilling round 26. |
| OR should not create extra GUI-only completion messages after a worker finishes; Paseo transcript/final response plus OR structured result are sufficient. | Avoid duplicative chatter solely for presentation. | User-accepted exploratory choice from grilling round 26. |
| Paseo may emit per-child notifications internally, but OR should aggregate normal fan-out completion for Main and wake Main only when the required batch is complete or a lane needs decision/has material failure. | This reduces unnecessary Main model rollouts while preserving event-driven execution. | User-accepted exploratory choice from grilling round 26. |
| The human operator should still be able to see each individual Scout/Researcher completion in Paseo even when Main receives an aggregated OR event. | Main efficiency and operator observability are separate concerns. | User-accepted exploratory choice from grilling round 26. |
| OR should annotate terminal worker/run outcomes through Paseo metadata/labels where practical instead of building a separate GUI status system. | Existing Paseo UI should remain the primary observability surface. | User-accepted exploratory choice from grilling round 26. |
| Archived completed OR workers should remain discoverable through normal Paseo history/retention while active workspaces/worktrees may be cleaned up. | Diagnostic history is useful even after filesystem isolation is removed. | User-accepted exploratory choice from grilling round 26. |
| Do not implement a custom OR composer pill/plugin UI in v1; rely on standard Paseo Subagents/history/labels initially, while keeping OR state/query contracts clean enough that a thin Paseo UI plugin can be added later if real mobile usage shows a gap. | Avoid duplicating existing Paseo UI before operator pain is demonstrated, without closing the extension path. | User-accepted exploratory choice from grilling round 26 after explanation. |
| OR v1 targets exactly one Paseo host/control plane for this deployment; cross-host scheduling and host selection are out of scope rather than merely deferred defaults. | The user expects one stable Paseo instance/host and does not need multi-host orchestration. | User-accepted exploratory choice from grilling round 27 with clarification. |
| OR v1 does not choose an execution host; it uses the single configured/current Paseo host. | Multi-host placement logic would add no value for the intended deployment. | User-accepted exploratory choice from grilling round 27. |
| OR v1 should not introduce a database; compact file-backed runtime journal/config is sufficient, with Paseo owning agent lifecycle and Git/PW owning canonical project state. | Avoid unnecessary infrastructure for a single-host runtime. | User-accepted exploratory choice from grilling round 27. |
| Recovery must not guess across arbitrary Paseo agents; OR should recover only workers attributable through OR metadata such as `or.run_id` plus its own known state. | Explicit identity is safer than heuristic adoption. | User-accepted exploratory choice from grilling round 27. |
| Loss of the OR journal must not make project recovery impossible when Paseo metadata/history and canonical Git/PW state still exist; recovery should be best-effort from those sources. | Runtime convenience state must remain disposable. | User-accepted exploratory choice from grilling round 27. |
| OR setup should manage only its own required worker execution profile/configuration and must not overwrite unrelated user Paseo profiles. | Runtime-owned configuration should stay scoped to OR. | User-accepted exploratory choice from grilling round 27. |
| Reserve a single OR-managed Paseo worker profile such as `or-worker` for the fixed worker model/effort policy. | A dedicated profile avoids drift and confusion with manually managed profiles. | User-accepted exploratory choice from grilling round 27. |
| Scout/Researcher/Worker/Reviewer semantics remain versioned in OR rather than being separate Paseo Agent Profiles; the Paseo profile supplies common execution settings only. | Semantic roles and execution profile are separate concerns. | User-accepted exploratory choice from grilling round 27. |
| Full environment readiness checks should run only at OR startup/reload, configuration change, explicit diagnostics, or after relevant failure; normal run launch performs only cheap task-local validation such as current repo/workspace/branch and required binding. | With one stable Paseo/Pi host, repeatedly probing already-running infrastructure would add latency/noise without useful protection. | Revised recommendation from grilling round 27; supersedes per-run full readiness checking. |
| Provide an explicit `doctor` diagnostic path for on-demand full validation of Paseo connectivity, Pi provider, OR worker profile/config, permissions and Git/worktree readiness. | Deep checks remain available when needed without taxing every run. | User-accepted exploratory choice from grilling round 27. |
| OR v1 should not run as a separate always-on daemon beside Paseo; Paseo already provides the long-lived control plane and OR should avoid duplicating that process role. | A second service would add deployment and lifecycle complexity without a demonstrated need. | User-accepted exploratory choice from grilling round 28. |
| The primary Main-facing OR integration should be a Pi extension exposing orchestration tools, backed by a separate testable OR core library. | This keeps Main integration ergonomic while preserving core separation. | User-accepted exploratory choice from grilling round 28. |
| Main shutdown does not justify adding an OR daemon solely to babysit workers; Paseo maintains worker lifecycle and later Main/OR recovery reconstructs state from OR journal, Paseo and Git. | Existing control-plane durability is sufficient for v1. | User-accepted exploratory choice from grilling round 28. |
| Routine Worker→Reviewer→repair mechanics may continue without another Main model rollout while OR is active, until a semantic decision, new authority or escalation is required. | Mechanical repair routing should not waste Main context or inference. | User-accepted exploratory choice from grilling round 28. |
| If Main/OR execution is entirely offline, v1 should not attempt autonomous repair-loop progression through a separate background service; completed worker state waits for Main/OR recovery. | Avoid introducing hidden autonomous orchestration solely for offline continuity. | User-accepted exploratory choice from grilling round 28. |
| OR should use one compact host-level runtime store/journal indexed by project and run identity rather than separate project-local runtime databases. | Runtime metadata is cross-project operational state, not project authority. | User-accepted exploratory choice from grilling round 28. |
| Role definitions, fixed worker profile, reasoning effort and global concurrency defaults should be global OR configuration; projects contribute only their own PW/task constraints and bounded authority. | Execution policy should not be duplicated across repositories. | User-accepted exploratory choice from grilling round 28. |
| Projects must not override the fixed worker model/profile in v1, though they may narrow task capabilities within OR role ceilings. | Preserve the single centrally managed worker execution profile. | User-accepted exploratory choice from grilling round 28. |
| OR configuration/extension changes should be reloadable without restarting the whole Paseo daemon or terminating unrelated Paseo sessions. | Runtime maintenance should not disrupt the broader control plane. | User-accepted exploratory choice from grilling round 28. |
| `or doctor` should be invokable through the Main-facing OR tool surface, not only from a terminal. | The user primarily operates through Paseo/Pi GUI rather than a shell. | User-accepted exploratory choice from grilling round 28. |
| The global concurrency ceiling of 9 applies to active OR subagents only and excludes Main. | Main is the orchestrator and should not consume the worker capacity budget. | User-accepted exploratory choice from grilling round 29. |
| Mandatory Research x3 waves should launch only when all three slots are available, rather than stagger one lane early and the rest later. | Simultaneous wave launch better preserves the intended independent fan-out semantics. | User-accepted exploratory choice from grilling round 29. |
| Mandatory broad-discovery Scout x2 waves likewise wait for two free slots and launch as a complete wave. | The same fan-out invariant should apply to required two-lane discovery. | User-accepted exploratory choice from grilling round 29. |
| Queue priority should favor continuation of existing execution chains (`repair`, `review`, `recheck`), then active PW obligations, then new Worker tasks, then new Scout/Research waves. | Finishing already-started work should normally outrank opening new exploratory work. | User-accepted exploratory choice from grilling round 29. |
| Do not reserve idle capacity for a Reviewer waiting on Worker repair; once repair completes, its recheck receives high queue priority instead. | Capacity should represent active sessions, not hypothetical future use. | User-accepted exploratory choice from grilling round 29. |
| Independent Worker fan-out may be batched to available capacity rather than requiring all Worker lanes to start simultaneously. | Unlike mandatory Research/Scout waves, Worker subtasks need not share a synchronized launch boundary when independent. | User-accepted exploratory choice from grilling round 29. |
| Within the same priority class, queueing should be primarily FIFO with simple anti-starvation aging for long-waiting legal tasks. | This keeps scheduling predictable without allowing indefinite starvation. | User-accepted exploratory choice from grilling round 29. |
| A single legal orchestration may consume all 9 subagent slots; v1 should not impose an additional per-run concurrency cap. | Extra per-run limits would add policy without demonstrated value. | User-accepted exploratory choice from grilling round 29. |
| A high-priority repair/review continuation may take capacity before a queued Research wave even when that wave is waiting for its full slot set. | Existing execution chains take precedence over starting new discovery. | User-accepted exploratory choice from grilling round 29. |
| Queued runs should expose explicit capacity state such as `queued: waiting for 3 slots` rather than appearing inert in `created`. | Operators and Main should be able to distinguish queueing from a stuck launch. | User-accepted exploratory choice from grilling round 29. |
| If a semantically valid result fails structured-schema validation, OR should allow one same-session format-correction attempt without rerunning the substantive task; a second failure becomes `schema_invalid`. | Formatting repair is distinct from semantic re-execution. | User-accepted exploratory choice from grilling round 30. |
| Schema-correction attempts do not count against the Worker's semantic repair-attempt budget. | Result-envelope repair should not consume implementation repair capacity. | User-accepted exploratory choice from grilling round 30. |
| On connection/transport interruption, OR should first reconnect/read back the existing Paseo agent/session before replacing it. | Preserve valid in-flight execution whenever the underlying session survives. | User-accepted exploratory choice from grilling round 30. |
| Retryable infrastructure failures receive at most two automatic retries after the initial failure, for a maximum of three infrastructure attempts. | A small bounded retry budget handles transients without masking persistent failures. | User-accepted exploratory choice from grilling round 30. |
| Infrastructure retries should use a short bounded backoff rather than immediate repeated attempts, without introducing a complex retry scheduler. | Avoid provider hammering while keeping recovery simple. | User-accepted exploratory choice from grilling round 30. |
| Permanent/configuration failures such as permission denial, missing model or invalid configuration must not be automatically retried. | Retry should be reserved for explicitly classified transient failures. | User-accepted exploratory choice from grilling round 30. |
| OR may retain one generous global hard execution ceiling as a safety fuse, but it must not treat normal long-running Paseo activity as timeout-worthy. | Hard timeout is last-resort protection, not ordinary scheduling policy. | User-accepted exploratory choice from grilling round 30. |
| V1 should use one global hard ceiling rather than role-specific timeout tuning. | Keep configuration small until real runtime data justifies differentiation. | User-accepted exploratory choice from grilling round 30. |
| Reaching the hard ceiling should trigger inspection/escalation rather than blind immediate agent termination; kill occurs only when the session is demonstrably stuck or explicitly cancelled. | Avoid destroying legitimate long-running work. | User-accepted exploratory choice from grilling round 30. |
| OR should maintain a small stable failure taxonomy including retryable provider failure, schema invalidity, permission/config failures, stale preconditions, side-effect uncertainty and runtime-internal failures. | Explicit classes make retry/recovery behavior deterministic and diagnosable. | User-accepted exploratory choice from grilling round 30. |
| Every assignment should carry an explicit resolved capability set such as repo read/write, shell, network and external-side-effect authority. | Runtime should know the effective execution envelope of each assignment. | User-accepted exploratory choice from grilling round 31. |
| OR should not attempt semantic inspection of every shell command to infer safety; use available platform enforcement plus explicit task/role contracts instead of building a custom shell sandbox. | Command-level semantic policing would be brittle and over-complex. | User-accepted exploratory choice from grilling round 31. |
| Where Paseo/Pi can mechanically disable unavailable tools/capabilities, OR should use that enforcement rather than relying only on prompt text. OR owns generous versioned default MCP/skill/extension bundles per role; Main declares only task-specific needs, OR checks those needs against role/PW boundaries, and Paseo/Pi materializes the resulting bundle. | Cheap native enforcement is preferable to building a new sandbox, and role bundles avoid making Main curate every worker from scratch while preventing an indiscriminate all-tools context. | User-accepted exploratory choice from grilling round 31, refined after discussion. |
| `network` authority is distinct from `external_side_effect` authority; network access alone must not imply permission to mutate external systems. | Reading/fetching and changing external state are different capability classes. | User-accepted exploratory choice from grilling round 31. |
| External-side-effect authority must be scoped to the concrete allowed effect, target or environment rather than granted as an unrestricted boolean permission. | Bounded side effects reduce accidental scope expansion while retaining Worker usefulness. | User-accepted exploratory choice from grilling round 31. |
| In PW mode, external-side-effect legality comes from the current PW obligation/authority; OR only carries and enforces that grant. | Runtime must not expand governance authority. | User-accepted exploratory choice from grilling round 31. |
| In ad-hoc mode, Main may grant a bounded external side effect when it follows directly from the user's request, without an extra confirmation step. | Explicit user intent such as deploy/restart already supplies the needed task authority. | User-accepted exploratory choice from grilling round 31. |
| Task capsules should reference required secret/credential bindings but must not embed secret values in prompts or transcripts; values are supplied through runtime/environment channels. | Keep credentials out of model-visible durable text where possible. | User-accepted exploratory choice from grilling round 31. |
| OR should apply simple redaction against known injected secret values before returning/logging worker output, rather than using an AI-based secret detector. | Deterministic redaction protects known credentials with minimal complexity. | User-accepted exploratory choice from grilling round 31. |

### Unresolved product/architecture decisions

- adopt, wrap, fork or reject `pi-extensible-workflows`;
- build a thinner Pi-native role runtime instead;
- exact Execution Obligation/Result schema;
- exact provider/capability adapter model;
- whether code-mode composition uses pi-fabric, MCP-native scripting or a smaller internal mechanism;
- whether optional typed classification has enough real volume/value to justify operation;
- final architecture/name beyond repository identity.

## Research status

Transferred findings are complete in:

`research/ORCHESTRATION_RUNTIME_PRIOR_ART_R1.md`

No Definition/Planning promotion has been authorized.

## Outcome

- Tentative conclusion: treat the new repository as the home of the provider-neutral execution/orchestration layer, with Project Workflow remaining external authority.
- Explicit product choices promoted: none.
- Research still needed before architecture selection: live substrate comparison/prototype after future Definition authorization.
- Next phase/action: approved transfer scope complete.
- Definition promotion authorization: pending
- Definition promotion subject: none

> Nothing in this file becomes accepted requirement/decision authority by itself.
